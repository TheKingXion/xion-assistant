# Xion Assistant DNS Routes

Use project-specific hostnames:

- Web and panel: `assistant.xion.<TU_DOMINIO>`
- API Worker: `api.asst.xion.<TU_DOMINIO>`

Avoid generic routes:

- Do not use `admin.<TU_DOMINIO>`.
- Do not use `api.<TU_DOMINIO>`.
- Do not use `app.<TU_DOMINIO>`.
